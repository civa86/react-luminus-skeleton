import React from 'react';

const Loader = () => {

    return (
        <div className="loader">
            <i className="ion-load-c animation-spin"/>
        </div>
    );
};

export default Loader;
